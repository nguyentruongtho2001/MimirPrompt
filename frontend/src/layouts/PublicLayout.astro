---
// Public Layout - Neo-Brutalism (Light Only)
import "../styles/global.css";

interface Props {
    title: string;
    description?: string;
    image?: string;
    type?: "website" | "article";
    canonical?: string;
}

const {
    title,
    description = "Khám phá bộ sưu tập AI Prompts được tuyển chọn cho Nano Banana",
    image = "/og-default.jpg",
    type = "website",
    canonical,
} = Astro.props;

const SITE_URL = import.meta.env.PUBLIC_SITE_URL || "http://localhost:4321";
const API_URL =
    import.meta.env.PUBLIC_POCKETBASE_URL || "http://127.0.0.1:8090";
const fullTitle = `${title} | MimirPrompt`;
const imageUrl = image.startsWith("http") ? image : `${SITE_URL}${image}`;
const canonicalUrl = canonical || Astro.url.href;
---

<!doctype html>
<html lang="vi">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content={description} />
        <title>{fullTitle}</title>

        <!-- Canonical -->
        <link rel="canonical" href={canonicalUrl} />

        <!-- Open Graph -->
        <meta property="og:title" content={fullTitle} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={imageUrl} />
        <meta property="og:type" content={type} />
        <meta property="og:url" content={canonicalUrl} />
        <meta property="og:site_name" content="MimirPrompt" />
        <meta property="og:locale" content="vi_VN" />

        <!-- Twitter Card -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta name="twitter:title" content={fullTitle} />
        <meta name="twitter:description" content={description} />
        <meta name="twitter:image" content={imageUrl} />

        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@500;600;700&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <!-- Header (Fixed) -->
        <header class="header">
            <div class="container header-inner">
                <a href="/" class="logo">
                    <span class="logo-text">MimirPrompt</span>
                </a>

                <nav class="nav">
                    <a href="/tags" class="nav-link">Tags</a>
                </nav>

                <!-- Mobile Menu Button -->
                <button
                    class="mobile-menu-btn"
                    id="mobileMenuBtn"
                    aria-label="Menu"
                >
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                    <span class="hamburger-line"></span>
                </button>
            </div>
        </header>

        <!-- Mobile Menu Overlay -->
        <div class="mobile-menu-overlay" id="mobileMenuOverlay"></div>

        <!-- Mobile Menu Panel -->
        <nav class="mobile-menu" id="mobileMenu">
            <div class="mobile-menu-header">
                <span class="mobile-menu-title">MimirPrompt</span>
                <button
                    class="mobile-menu-close"
                    id="mobileMenuClose"
                    aria-label="Close menu"
                >
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="20"
                        height="20"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2.5"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    >
                        <line x1="18" y1="6" x2="6" y2="18"></line>
                        <line x1="6" y1="6" x2="18" y2="18"></line>
                    </svg>
                </button>
            </div>
            <div class="mobile-menu-links">
                <a href="/" class="mobile-link">🏠 Trang chủ</a>
                <a href="/tags" class="mobile-link">🏷️ Tags</a>
            </div>
        </nav>

        <!-- Main -->
        <main class="main">
            <slot />
        </main>

        <!-- Footer -->
        <footer class="footer">
            <div class="container footer-inner">
                <span class="footer-brand">MimirPrompt</span>
                <p class="footer-text">AI Prompt Gallery · Made with care</p>
            </div>
        </footer>

        <style>
            /* Header */
            .header {
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                width: 100%;
                z-index: 1000;
                background: white;
                border-bottom: 2px solid #1a1a1a;
            }

            .header-inner {
                display: flex;
                align-items: center;
                justify-content: space-between;
                height: 64px;
            }

            .logo {
                display: flex;
                align-items: center;
                gap: 8px;
                color: #1a1a1a;
                text-decoration: none;
            }

            .logo:hover {
                text-decoration: none;
            }

            .logo-icon {
                font-size: 1.5rem;
            }

            .logo-text {
                font-family: var(--font-display);
                font-size: 1.25rem;
                font-weight: 700;
            }

            .nav {
                display: flex;
                align-items: center;
                gap: 4px;
            }

            .nav-link {
                padding: 8px 16px;
                font-size: 0.875rem;
                font-weight: 500;
                color: #1a1a1a;
                border-radius: 4px;
                transition: 0.15s ease;
            }

            .nav-link:hover {
                background: #f5f5f0;
                text-decoration: none;
            }

            /* Main */
            .main {
                min-height: calc(100vh - 64px - 120px);
                padding-top: 64px;
            }

            /* Footer */
            .footer {
                background: #1a1a1a;
                color: white;
                padding: 48px 0;
                margin-top: 64px;
            }

            .footer-inner {
                text-align: center;
            }

            .footer-brand {
                display: block;
                font-family: var(--font-display);
                font-size: 1.125rem;
                font-weight: 700;
                margin-bottom: 8px;
            }

            .footer-text {
                color: rgba(255, 255, 255, 0.6);
                font-size: 0.875rem;
            }

            /* Mobile Menu Button */
            .mobile-menu-btn {
                display: none;
                flex-direction: column;
                justify-content: center;
                gap: 5px;
                width: 40px;
                height: 40px;
                padding: 8px;
                background: none;
                border: 2px solid #1a1a1a;
                border-radius: 4px;
                cursor: pointer;
                transition: 0.15s ease;
            }

            .mobile-menu-btn:hover {
                background: #f5f5f0;
            }

            .hamburger-line {
                display: block;
                width: 100%;
                height: 2px;
                background: #1a1a1a;
                border-radius: 1px;
                transition: 0.2s ease;
            }

            .mobile-menu-btn.active .hamburger-line:nth-child(1) {
                transform: rotate(45deg) translate(5px, 5px);
            }

            .mobile-menu-btn.active .hamburger-line:nth-child(2) {
                opacity: 0;
            }

            .mobile-menu-btn.active .hamburger-line:nth-child(3) {
                transform: rotate(-45deg) translate(5px, -5px);
            }

            /* Mobile Menu Overlay */
            .mobile-menu-overlay {
                position: fixed;
                inset: 0;
                background: rgba(0, 0, 0, 0.5);
                z-index: 998;
                opacity: 0;
                visibility: hidden;
                transition: 0.3s ease;
            }

            .mobile-menu-overlay.active {
                opacity: 1;
                visibility: visible;
            }

            /* Mobile Menu Panel */
            .mobile-menu {
                position: fixed;
                top: 0;
                right: -280px;
                width: 280px;
                height: 100vh;
                background: white;
                border-left: 2px solid #1a1a1a;
                box-shadow: -4px 0 20px rgba(0, 0, 0, 0.1);
                z-index: 999;
                transition: 0.3s ease;
                overflow-y: auto;
            }

            .mobile-menu.active {
                right: 0;
            }

            .mobile-menu-header {
                display: flex;
                align-items: center;
                justify-content: space-between;
                padding: 16px 20px;
                border-bottom: 2px solid #1a1a1a;
                background: var(--accent-secondary, #fef08a);
            }

            .mobile-menu-title {
                font-family: var(--font-display);
                font-size: 1.125rem;
                font-weight: 700;
            }

            .mobile-menu-close {
                width: 32px;
                height: 32px;
                display: flex;
                align-items: center;
                justify-content: center;
                background: white;
                border: 2px solid #1a1a1a;
                border-radius: 4px;
                font-size: 1rem;
                cursor: pointer;
                transition: 0.15s ease;
            }

            .mobile-menu-close:hover {
                background: #1a1a1a;
                color: white;
            }

            .mobile-menu-links {
                padding: 16px;
                display: flex;
                flex-direction: column;
                gap: 8px;
            }

            .mobile-link {
                display: flex;
                align-items: center;
                gap: 10px;
                padding: 14px 16px;
                background: #f5f5f0;
                border: 2px solid #1a1a1a;
                border-radius: 4px;
                font-size: 1rem;
                font-weight: 500;
                color: #1a1a1a;
                text-decoration: none;
                transition: 0.15s ease;
            }

            .mobile-link:hover {
                background: var(--accent-primary, #4f46e5);
                color: white;
                text-decoration: none;
            }

            /* Responsive */
            @media (max-width: 768px) {
                .nav {
                    display: none;
                }

                .mobile-menu-btn {
                    display: flex;
                }

                .header-inner {
                    height: 56px;
                }

                .main {
                    padding-top: 56px;
                }
            }
        </style>

        <script define:vars={{ API_URL }}>
            window.API_URL = API_URL;
        </script>

        <script>
            // Mobile Menu Toggle
            const mobileMenuBtn = document.getElementById("mobileMenuBtn");
            const mobileMenuClose = document.getElementById("mobileMenuClose");
            const mobileMenu = document.getElementById("mobileMenu");
            const mobileMenuOverlay =
                document.getElementById("mobileMenuOverlay");

            function openMobileMenu() {
                mobileMenuBtn?.classList.add("active");
                mobileMenu?.classList.add("active");
                mobileMenuOverlay?.classList.add("active");
                document.body.style.overflow = "hidden";
            }

            function closeMobileMenu() {
                mobileMenuBtn?.classList.remove("active");
                mobileMenu?.classList.remove("active");
                mobileMenuOverlay?.classList.remove("active");
                document.body.style.overflow = "";
            }

            mobileMenuBtn?.addEventListener("click", openMobileMenu);
            mobileMenuClose?.addEventListener("click", closeMobileMenu);
            mobileMenuOverlay?.addEventListener("click", closeMobileMenu);

            // Close menu on escape key
            document.addEventListener("keydown", (e) => {
                if (e.key === "Escape") closeMobileMenu();
            });
        </script>
    </body>
</html>
